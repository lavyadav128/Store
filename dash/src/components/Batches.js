import React from 'react';
import BatchManager from './BatchManager'; // adjust path if BatchManager.jsx lives elsewhere

const Batches = () => {
  return <BatchManager />;
};

export default Batches;