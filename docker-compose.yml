services:
  backend:
    build: ./back
    container_name: backend_container
    ports:
      - "3000:3000"

  frontend:
    build: ./dash
    container_name: frontend_container
    ports:
      - "3001:3001"